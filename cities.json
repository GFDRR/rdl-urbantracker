---
---
[{% for city in site.cities %}
  {
    "city_id": {{ city.city_id | jsonify }},
    "title": {{ city.title | jsonify }},
    "country": {{ city.country | jsonify }},
    "flag": {{ city.flag | jsonify }}
  }{% unless forloop.last %},{% endunless %}{% endfor %}
]
